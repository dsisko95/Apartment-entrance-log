import { FilterClientNamePipe } from './filter-client-name.pipe';

describe('FilterClientNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClientNamePipe();
    expect(pipe).toBeTruthy();
  });
});
