import { FilterAddressPipe } from './filter-address.pipe';

describe('FilterAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
